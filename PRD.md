# SpecAssay — Product Requirements (ID Registry)

This PRD is the **authoritative ID registry** for the `specassay` repo's own
work — the tool dogfooding itself. Founded 2026-08-18 by the docs room
(design-room ruling), scoped explicitly as **founding, not backfill**: no row
below is minted `proven` by declaration. Every ID here rides as **anointed
backlog** (registry entry + one open `**Carries**:` TODO in
`specs/backlog/tasks.md`) until real work picks it up — the registry opens
honest-empty and earns its greens from there, per Rule 6a.

SpecAssay Gate 2 enforces the exact-set rule: the IDs defined here must equal
the IDs referenced in `specs/` and `tasks.md` (the one exception is
*anointed backlog*, an ID carried only by an open TODO).

## DOCS

First citizens of this registry — new work, minted 2026-08-18 (US-DOCS-10,
anointed backlog: reach a working Gate from the public quickstart alone).

- US-DOCS-10 — As a cold installer, I want to reach a working Gate on my own repo from the public quickstart alone, so adoption needs no author in the loop.
- FR-DOCS-10 — Quickstart: install, config, first check, first honest red.
- FR-DOCS-20 — Trace-manifest format reference, versioned with the schema.
- FR-DOCS-30 — Troubleshooting built from real failure classes, each entry citing the incident that taught it.
- FR-DOCS-40 — Every documented behavior cites the registry row(s) it documents.
- AC-DOCS-10 — Given a cold installer with no prior context, when following the public quickstart alone, then a working Gate on their own repo within 30 minutes, zero questions asked of the author.
- FR-DOCS-50 — Once FR-GATE-40 ships, restore `docs/**` to `specassay-check-config.yml`'s `src_globs`. Companion to FR-GATE-40, minted the same day: the current exclusion is a tracked temporary, not a settlement.
- FR-DOCS-60 — Onboarding ladder rung one: a paste-block walkthrough from a blank agent session to a first Thread Report in one short sitting, on a throwaway project, with a refusal demonstrated inside the sitting rather than described.
- FR-DOCS-70 — A version number in a governed document says which kind of claim it is making, and the release refuses one that does not. Four instances of one failure prompted this: a version observed once, typed into a sentence, then left to rot while the thing it described moved, each caught by a person noticing and each later than the last. The mechanism is deliberately not a generator. A generated receipt is unfalsifiable by its reader: they cannot check it against anything, only trust the generator, which makes the documentation a self-report, the one epistemic class this tool exists to refuse. So the receipt stays human-written and the machine refuses it when it lies. Three rules: no unclassified version token in prose, a current claim equals the version being cut, and a dated observation carries an age measured in releases that refuses past a threshold unless the page states why it is kept. The classification is the load-bearing rule; the other two are enforcement on top of a distinction that did not previously exist. Minted 2026-09-18 on a ruling, after the proposed single equality rule was shown to catch only one of the four instances: the README line that started it said "Verified 2026-09-04 ... v0.4.13", which was true, stayed true, and carried its own date. The failure was staleness of relevance, not falsity, and only an age rule sees it. <!-- specassay:stale-ok the record of what was true at v0.4.13; re-observing it would mean re-running on that release, which is not what the sentence claims -->
  - AC-DOCS-20 — Given a governed document containing a version token, when the check runs, then a token carrying none of the accepted classifications is refused naming file, line and token; a token marked as a current claim that does not equal the version being cut is refused; a dated observation more releases behind that version than the threshold is refused unless the page carries a stated reason to keep it; and a quoted range is refused when it disagrees with the bundle manifest.

## GATE

Migrated from `docs/backlog.md`'s existing anointed items 2026-08-18.
Coverage basis: registration of existing intent, not newly attributed — the
asks below predate this mint; see `docs/backlog.md` for the full narrative
each was minted from.

- FR-GATE-10 — `--matrix`: regenerate a human-readable coverage table (`coverage.md`) + SVG summary bar (`coverage.svg`) from the current registry/gate state. **Boundary line (design commitment, stated before build):** `--matrix` is a portfolio-snapshot renderer, not a second viewer. It reads the same already-computed data the Gate just derived — never re-scans, never re-derives status logic of its own — and writes two static, committed artifacts meant for a cold reader with no Loupe running: a CI log, a PR diff, a README badge. No live interactivity, no browser, no drift risk beyond ordinary file staleness: CI enforces the golden thread via the Gate script itself, every push, regardless of whether `coverage.md`/`coverage.svg` were regenerated that push. If the SVG and Loupe's live rendering of the same manifest ever disagree, the manifest is what's wrong, not either renderer. Reads `trace-manifest.json` (v4, the frozen four-status file) — `retired` rows are absent from `rows[]` there by design (`FR-GATE-30`), so the matrix never needs a fifth bar segment. Colors and canonical ordering are the family's own status tokens (`INTERFACE-CANON.md` §2: backlog `#9ed4ff` → tracked-debt `#c9903a` → proven `#219653` → GAP `#eb5757`, GAP last), not invented locally. Self-dating: the SVG carries the manifest's own `generatedAt` timestamp as visible text, the same discipline every other family artifact with a "when was this true" claim already follows. Modeled on HomesFlow's own bespoke `--matrix` mode (`scripts/check-traceability.sh`, pre-canon prior art for the structure — summary table, per-type sections, generated-file banner — not for its legacy colors) — feature parity here is this repo's own named trigger for that fork's retirement.
  - AC-GATE-10a — Given `--matrix`, when the Gate runs, then `coverage.md` and `coverage.svg` are written from that same run's already-computed rows and status counts, with no second scan of the target.
  - AC-GATE-10b — Given the emitted SVG, when inspected, then its status-color segments use exactly the family's canonical tokens and ordering (backlog `#9ed4ff` → tracked-debt `#c9903a` → proven `#219653` → GAP `#eb5757`), and its visible text carries the manifest's own `generatedAt` timestamp.
  - AC-GATE-10c — Given a registry with one or more retired IDs, when `--matrix` runs, then those IDs are absent from both `coverage.md`'s tables and `coverage.svg`'s bar — the same absence `trace-manifest.json`'s own `rows[]` already has, not a separately-handled case.
- FR-GATE-20 — Portfolio-snapshot mode (`--portfolio`, `portfolio-snapshot.md`): the same single-repo coverage data `--matrix` already computes, reframed for a cold reader — a stakeholder or new joiner with zero prior context, not a CI consumer. **Three guardrails, ratified before build:** (1) **Name the audience.** Every rendering choice (plain-prose framing, an explanatory opening paragraph, no `GENERATED FILE — do not edit` CI banner, no jargon-first tables) answers to that cold reader specifically, not to whoever `--matrix` already serves. (2) **Same boundary line as `FR-GATE-10`.** A document, never a viewer: no interactivity, no scripting; if anything renders visually (it may embed `coverage.svg` rather than generate a second image), family color tokens and self-dating apply the same way. (3) **Scope the word.** "Portfolio" here means *this repo's own whole thread portfolio* — every registry ID, one repo — stated explicitly so it isn't misread later. Cross-repo aggregation (a real, different, bigger feature) is explicitly out of scope; if that need ever arrives, it mints as a **new** row in a future federation era, never a reinterpretation of this one. This fence is recorded here specifically so nobody reads `FR-GATE-20` as having already answered the cross-repo question.
  - AC-GATE-20a — Given `--portfolio`, when the Gate runs, then `portfolio-snapshot.md` is written from that same run's already-computed rows and status counts (no second scan), opens with a plain-prose paragraph naming what the document is and who it's for, and carries no `GENERATED FILE — do not edit` CI-style banner.
  - AC-GATE-20b — Given any target repo, when `--portfolio` runs, then the emitted document names only that one repo's registry and never implies or claims coverage of any other repo.
- FR-GATE-30 — `retired`: a genuine fifth status alongside proven/tracked-debt/backlog/GAP, distinguishing an intent withdrawn on purpose from a silent GAP. There is no settable status field: `retired` derives entirely from an explicit, dated, reasoned, append-only retirement record on an open task — `**Retires**: <id-list> (<YYYY-MM-DD>): <reason>`, parallel to `**Carries**:` — checked ahead of every other status branch, for any type (US/FR/NFR/AC). Version boundary: `trace-manifest.json` (v4) freezes at exactly four status values (a published schema with strict validators); a retired row leaves v4's `rows[]` entirely rather than carry a value v4 never declared, and the same information moves to a new top-level `retired: [{id, date, reason}]` list instead, so nothing disappears silently. `trace-manifest.v5beta.json` carries `retired` as a normal fifth row status from the start. A malformed retirement record (no parseable ID, or no `(YYYY-MM-DD): reason` tail) refuses loudly before any scanning, the same discipline as `FR-GATE-70`. Minted 2026-08-18 (HomesFlow's first intent retirement, `US/FR/AC-CLEW-01`), design settled 2026-08-20 across two design-room rulings after a modeling proposal (four options weighed; a genuine fifth status won on the evidence already in `docs/backlog.md`'s "Pattern candidate" section) and a self-audit correction on the v4 export mapping. Built citing `docs/backlog.md`'s four-legs framework (registry annotation, spec anchor, carry task, gap-vs-withdrawn at proof) and HomesFlow's own hand-improvised tombstone convention as prior art.
  - AC-GATE-30a — Given an open task carrying a well-formed `**Retires**:` record naming one or more registry IDs, when the Gate runs, then every named ID's status is `retired` in `trace-manifest.v5beta.json`'s `rows[]`, regardless of its proof or pending state.
  - AC-GATE-30b — Given the same retired ID, when `trace-manifest.json` (v4) is emitted, then that ID does not appear in `rows[]`, appears exactly once in a top-level `retired` list with its date and reason, and `statusCounts` carries exactly the four original keys (no `retired` key).
  - AC-GATE-30c — Given a `**Retires**:` record with no parseable registry ID or no `(YYYY-MM-DD): reason` tail, when the Gate runs, then it refuses before any scanning and writes no manifest.
- FR-GATE-40 — `orphan-covers` gains domain scoping consistent with `orphan-spec` and `orphan-task`, so `@covers` detection distinguishes use from mention: marks inside fenced code blocks, documentation files, and the checker's own comments are not live marks. Minted 2026-08-18, citing the DOCS-room founding finding: `docs/**` had to be excluded from the assayer's own self-governance because the Gate could not yet read its own documentation without believing it (`docs/docs-gaps.md`).
  - AC-GATE-40 — Given a docs file quoting an `@covers` line as a teaching example, when the Gate runs with docs in `src_globs`, then no `orphan-covers` finding derives from the quotation.
  - AC-GATE-41 — Given `check-traceability.sh`'s own source, when scanned, then its comments and regex definitions never self-match.
- FR-GATE-50 — The manifest emitter dedupes `implementations` (and `proofs`) on `(id, normpath, line)` before writing the manifest, so the same mark reached twice (overlapping `src_globs` entries, or a `./x` vs `x` path spelling) is listed once. Minted 2026-08-18, citing a real emit where 62 of 100 rows carried duplicated implementations.
  - AC-GATE-50 — Given the same `@covers` mark reachable via two overlapping `src_globs` entries or a `./x` vs `x` path spelling, when the manifest is emitted, then that mark appears exactly once in `implementations`.
- FR-GATE-70 — A list-type config key (`src_globs`, `test_globs`, or any later `yaml_list()` consumer) that's present but malformed (an inline YAML array) or bare (no items under it) refuses loudly before any scanning, instead of silently parsing to an empty list. Minted 2026-08-19: the self-referential case of the silent-gap shape this tool exists to refuse in adopters' own work — found twice in one day, once in the docs room's own reproduction recipe and once in the capture session that followed it.
  - AC-GATE-70a — Given a list-type config key written as an inline array, when the Gate runs, then it refuses before any scanning: stderr names the key, quotes the offending raw line, shows the accepted block-style shape, and points at the troubleshooting entry — no `trace-manifest.json` is written.
  - AC-GATE-70b — Given a list-type config key present with no items under it, when the Gate runs, then it refuses the same way, and the message states plainly that omitting the key entirely is what means "none."
  - AC-GATE-70c — Given a list-type config key absent from the file entirely, when the Gate runs, then behavior is unchanged: an empty list, no refusal.
- FR-GATE-60 — An additive `provenVia` (delegated-proof) field on FR/US rows, naming the AC(s) whose proof stands in for a row that carries no `@covers`/test of its own, so Loupe's PROOF stage can render the chain instead of "No proof." Minted 2026-08-18, design-room proposal, considered not built: **open design question** — the registry has no existing FR→AC parent/child edge (shared ID-domain prefix is a naming convention, not a declared relationship), so `provenVia` needs either (a) an explicit author-declared link (e.g. a `**Provenvia**:`/registry-annotation grammar, honest but one more thing to write) or (b) an inferred one (the emitter guesses from domain/statement adjacency, convenient but a real risk of a false chain — exactly the kind of self-report Rule 6a exists to refuse). No AC yet; needs that ruling first.
- FR-GATE-80 — `proofs[]`-population (the `test_ac_regex` scan feeding `proof_by`/the manifest's `proofs` field) matches anywhere in a test file's text, including comments, with no structural check that the match sits on a real test — and no filtering against the passing-testcase set even when `test_results` (Rule 6a) is configured, unlike `status_for()`'s own `tested` set, which inherits that filter. Minted 2026-08-20, citing a SpecCost engine finding (not acted on there — SpecCost doesn't own this code, this repo does): `AC-WALK-10`'s `proofs[]` lists `test_AC_WALK_10_new_activity_reflected_with_no_separate_refresh`, a name that survives only inside a rename-explanation comment after the real test was renamed (2026-08-18 mark-provenance audit, T016). No status-integrity risk — `status_for()`'s protected path already keeps `AC-WALK-10` honestly `tracked-debt` — but a legibility/trust defect on a field whose whole job is being trustworthy at a glance, and the same root cause as `FR-GATE-40` (text-matching where structural/execution-matching is required), one call site over. Verified directly against this repo's own `check-traceability.sh` (lines ~330–347): `test_acs.txt` is `cut` straight from the same unfiltered `proof_hits.txt` that populates `proof_by`, not independently derived. Recommend building alongside any future `FR-GATE-40`-family work rather than as a separate pass — one root cause, two call sites.
  - AC-GATE-80 — Given `test_results` is configured and a test name matches `test_ac_regex` only inside a comment (not a real, currently-passing test), when the manifest is emitted, then that name does not appear in the ID's `proofs[]`.
- FR-GATE-90 — Parentage: the Gate derives an optional `parent` edge per row at emit time from the registry document's own heading/section nesting — never authored, never inferred by a viewer, never guessed from ID-prefix naming convention. Per-project opt-in via `parent_derivation: heading-nesting | none` in config; a flat spec (or `none`) emits no edges, and absence renders as absence (em-dash law), not a guess. Document structure yields a tree by construction — single-parent v1, for free; multi-parent/DAG deferred until a real cross-cutting case forces it. Every row with descendants carries a composition rollup over **all rows in its subtree, at any depth** — not leaves-only: a non-leaf's own status counts alongside its descendants', so a mid-tier `GAP` or `backlog` can't vanish from an ancestor's rollup the way a leaves-only count would let it (ratified rationale: an invisible gap is the one failure class this system exists to prevent). The rollup carries a total row count alongside its per-status counts, so a renderer can name its basis ("N rows") rather than imply a single meaning for a mixed-depth count. **Cited invariant, not incidental:** the recursive rollup is cycle-safe only because single-parent holds (a tree has no cycles by construction); anyone relaxing single-parent to a DAG later must revisit rollup safety before shipping it, not discover it in production. Minted 2026-08-20 from `docs/hierarchy-parentage-brief-2026-08-20.md` (design room, two-question ruling — derivation mechanism and tree-vs-DAG were closed by the brief's own "Resolution on population" section; rollup basis and sequencing ruled separately the same day), citing the acceptance criterion that forced this brief: "if I can't find code and test from intent, then this whole system does not really help me" (Loupe's descent on `US-UNBIND-10` dead-ending at "No proof" despite real proof three levels down, on `AC-UNBIND-10/20/30`). Sequenced behind DIG and `T905`/`T906` (parentage rides behind the emission work it resembles); anointed backlog until then. The Loupe-side COMPOSITION card that renders this edge is out of this repo's scope, scheduled separately — nothing here blocks on it: the edge ships in the manifest whether or not a viewer renders it yet.
  - AC-GATE-90a — Given a registry document with heading/section nesting and `parent_derivation: heading-nesting` configured, when the Gate runs, then each nested row's manifest entry carries a `parent` edge to its enclosing row, and a flat document (or `parent_derivation: none`) carries no `parent` edges at all.
  - AC-GATE-90b — Given a row with descendants at more than one level deep, when the manifest is emitted, then that row's composition rollup counts include every descendant row at every depth, not only its direct children, and include the row's direct children's own statuses too (not only their descendants').
  - AC-GATE-90c — Given any row's composition rollup, when the manifest is emitted, then the rollup carries a total row count alongside its per-status counts.
- FR-GATE-100 — Cold-install on-ramp: the Gate talks a first-time installer through its own state instead of leaving them to infer it. Three parts. (1) An empty registry (0 IDs) still exits 0, but the closing line says the registry is empty, that nothing is promised yet, that the Gate stays green until something is, and names the two on-ramps to a first ID with runnable commands: greenfield (mint before the Spec Kit spec; the preset makes specs inherit IDs from the registry) and brownfield (mint one ID against a requirement in a doc that already exists; no backfill). (2) The Python interpreter is detected, never hardcoded: `SPECASSAY_PYTHON`, then `python3`, then `python`, each probed for 3.8 or newer; none usable is a one-line FAIL with an install hint, exit 2, before any scanning. (3) The script reports its own config state at startup, every run: the config path it found and how, or that it is missing, that it is running on `config-template.yml` defaults, and the one `cp` command that scaffolds it. Minted 2026-09-03 from the first Windows cold install on record (senior engineer, Git Bash, no Spec Kit experience, brownfield repo with specs under `docs/**`): his run ended at `OK (0 registry IDs)` with no idea what came next, his box had `python` but no `python3`, and he could not tell whether install had scaffolded the config.
  - AC-GATE-100a — Given a registry file with zero definition lines and an otherwise clean run, when the Gate finishes, then it exits 0 and its closing output states the registry is empty, that the Gate stays green until a first ID exists, and gives runnable next steps for both a greenfield and a brownfield first mint.
  - AC-GATE-100b — Given a machine where `python3` is absent or unusable but `python` is Python 3.8 or newer, when the Gate runs, then it completes on `python` and reports which interpreter it used; given neither is usable, then it fails before any scanning with exit 2 and a one-line install hint, and writes no manifest.
  - AC-GATE-100c — Given any run, when the Gate starts, then it reports the config path it resolved and how; given the config file is missing, then it names the missing path, states it is running on `config-template.yml` defaults, and prints the copy command that scaffolds it.
- FR-GATE-110 — A configured `test_results` report that carries no test cases at all, or that the reader cannot parse, refuses the run (exit 2, no manifest) instead of being read as a verdict. Rule 6a makes a currently-passing test the difference between `proven` and `tracked-debt`, so a report with zero testcases is an unusable input, not news: believing it turns a harness that died before its first test, a wrong path, or a writer that emitted the wrong file into a wall of demotions that reads like honest reporting, under `executionVerified: true`. The refusal says what is wrong with the file, so the operator looks at the harness rather than at the registry. Minted 2026-09-16 from the Chalkup trial (Swift 6.3.3, `swift test --xunit-output` writing only the swift-testing file, zero cases, for an XCTest-only run): whose bug the empty file is belongs to that toolchain, but trusting it was ours. Companion rule to FR-GATE-70: an input that cannot mean anything refuses loudly rather than parsing to nothing.
  - AC-GATE-110 — Given `test_results` points at a well-formed report containing no testcase elements, when the Gate runs, then it refuses with exit 2, states that the report has no test cases, and writes no manifest.
- FR-GATE-120 — Two registry IDs that reduce to the same key under proof matching are reported as a failing finding (`ambiguous-id-key`), not silently resolved. Proof matching is separator-insensitive by construction, because a test name cannot carry an ID's punctuation: `AC-1-2` and `AC-12` are one key, so a test named for either could be credited to the wrong row. Which row a proof belongs to is the registry's to settle, never the engine's to guess, and a wrong guess is the silent-green failure this tool exists to refuse, one layer down. Minted 2026-09-16 alongside the proof-direction repair that made non-stock grammars reachable at all: separator-insensitive lookup is what makes a dotted or underscore-bearing grammar provable, and this is the one case it cannot serve. Upgrade note, not a registry claim: a registry v0.4.13 accepted can fail here, because v0.4.13 never looked. <!-- specassay:stale-ok the record of what was true at v0.4.13; re-observing it would mean re-running on that release, which is not what the sentence claims -->
  - AC-GATE-120 — Given a registry declaring two IDs that differ only in punctuation, when the Gate runs, then it fails with an `ambiguous-id-key` finding naming both IDs, rather than crediting a proof to either.
- FR-GATE-130 — `mint-id.sh` refuses to compose an ID the project's configured `id_regex` does not admit, instead of issuing scope its own checker will reject. The decade scheme the minter implements is the stock `TYPE-DOMAIN-NN` grammar written out as code; a project that configures another grammar gets an ID that looks minted and fails the Gate the moment it is used, with nothing naming the helper that issued it. The refusal quotes the configured `id_regex`, shows the ID it would have composed, and says to mint by hand into the registry in the grammar the config declares. Minted 2026-09-16 from the same Chalkup trial: the estate's dotted grammar made every mint the helper could produce unusable, silently. Consistent with the class the 0.5.0 release settles: wherever a configured grammar is consumed, it is consumed as configured.
  - AC-GATE-130 — Given a project whose configured `id_regex` does not admit `TYPE-DOMAIN-NN`, when `mint-id.sh` is asked for an ID, then it exits non-zero naming the configured grammar and the ID it declined to compose, and appends nothing to the registry.

- FR-GATE-140 — A task is one logical line to every scan, however many physical lines it occupies. A `**Carries**:` list that soft-wraps onto continuation lines carries the same IDs it would carry unwrapped, and the mark itself counts wherever in the task it sits. Before this, two scans read one file and disagreed: the exact-set scan read every line and counted a wrapped ID as claimed, while the pending scan read only the physical `- [ ]` line and never saw it, so the ID reported as a silent gap with nothing to say why. The verdict was not a gap; it was two parsers differing, which is the capture-method failure `PROMOTION-CONTRACT.md` rule 12 names, inside the tool that exists to refuse it. A continuation is an indented, non-blank line that is not itself a list item; a nested bullet, a blank line, a heading and any new top-level bullet all end the fold. **Ruled fold rather than refuse:** refusing a wrapped list was the alternative, and this estate had already answered the same question in `scripts/check-doc-versions.py`, where a mark that covered only its own line was recorded as a bug because it made an author break their line wrapping to satisfy the checker. Refusing would also red every adopter whose `tasks.md` already wraps, on a formatting choice Markdown calls meaningless, and would put the parser's limitation on the author. This tool refuses dishonesty, not typography. Minted 2026-09-20 from the Bang rehearsal on v0.5.1, where three acceptance criteria reported as silent gaps for no reason a reader could find.
  - AC-GATE-140 — Given an open task whose `**Carries**:` list wraps onto a continuation line, when the Gate runs, then every ID in that list reaches the same verdict as it would unwrapped, the pending scan and the exact-set scan agree on the set the task carries, and a task whose mark sits on a continuation line is not reported as missing its Carries field.

- FR-GATE-150 — A line ending is not a verdict. Every file the Gate writes and then reads again is written with `\n` and read with any `\r` stripped, so the same project reaches the same verdict on Windows as on Linux, and a file that picks up CRLF anywhere between the write and the read still matches. Before this, the junit filter rewrote `test_acs.txt` through Python's text mode, which on Windows emits `\r\n`, and the Bash comparisons that follow (`grep -qx` against the registry, `comm -23` against the covers set) compared `AC-UI-10\r` with `AC-UI-10` and found no match. The manifest's own reader strips per line, so the statuses stayed right while the failures beside them were impossible: eleven `untraced scope (test name)` refusals on IDs the same manifest called `proven`, with `GAP: 0`. That is the capture-method failure `PROMOTION-CONTRACT.md` rule 12 names, one platform over: two readers of one file disagreeing, and the tool reporting the disagreement as a fault in the work. **Both ends are fixed, on purpose:** either alone closes the reported bug, and neither alone survives the other end being replaced by an older extension, a shim, or an editor. The same stripping is owed by any text file the Gate reads and does not write, which is why the config readers strip too. Minted 2026-09-24 from the first Windows run of the Bang walk, Git Bash and Python 3.12.10.
  - AC-GATE-150 — Given a handoff file the Gate wrote with CRLF, when the Gate reads it back, then every ID in it matches the registry exactly as it would with LF: no `untraced scope` failure is raised for a registered ID, no `uncovered proof` diagnostic is raised for an ID whose `@covers` line names it, the row's status is unchanged, and no carriage return reaches the manifest.
  - AC-GATE-150b — Given a `specassay-check-config.yml` checked out with CRLF, when the Gate runs, then it produces the same rows and the same verdict as the identical config with LF, rather than refusing with `registry not found` against a file that exists.

## DIG

Archaeology mode, rung (a) of the three-rung ladder (no-LLM floor only;
local-model and user-supplied-API-key rungs are explicitly out of scope).
Built 2026-08-20 per `docs/archaeology-mode-build-handoff-v1-2026-08-20.md`
(receipted by version line before this mint).

- US-DIG-10 — As a stranger dropped into an unfamiliar, undocumented repo, I want a first-pass structural read of its likely intent/behavior/proof shape, so I have a starting map instead of nothing.
- FR-DIG-10 — `specassay dig [path]` command: static, no-LLM heuristics over test names/bodies, routes/API surface, module/directory structure, README/docs prose, and commit history propose a first-pass candidate registry — written only to `dig-report.json`, flat data the Gate never loads, never chains, never governs anything with. There is no `--anoint` flag on this command; turning a proposed row into a real, dated, minted registry row is separate, later, human work. **Amended 2026-08-21:** the default output location is the operator's current working directory, never the scanned target path and never a session-scratch directory — found running the command for real against an external, read-only clone (`github.com/SpecDriven/insurance-java`, per the archaeology-mode build handoff), where the report was first written to a session scratchpad "under our own control" per the handoff's own §0 caution about not writing into the target, except a scratchpad is cleanup-eligible and not actually durable. A receipt whose evidence sits in cleanup-eligible ground is a claim on a timer, not a record — the tool's own default now makes the durable choice the easy one instead of leaving it to whoever invokes it that session.
  - AC-DIG-10 — Given any target repo, when `dig` runs (dry-run or real), then no write touches any file outside the configured output path — no exceptions, no `--anoint` flag exists.
  - AC-DIG-20 — Given `--dry-run`, when `dig` runs, then it prints row counts by type and source and writes nothing at all, not even a partial file.
  - AC-DIG-30 — Given no `--out` override, when `dig` runs, then the report is written to `dig-report.json` in the operator's current working directory, not the scanned target path — the default output location depends on where the operator invoked the command from, never on what path they pointed `dig` at.
- FR-DIG-20 — Dig-report schema: each row carries a type guess (US/FR/AC/NFR), statement text, `epistemicClass: "inferred"` (constant, always present, never omitted), provenance (source kind, file, line, and for test-derived rows the test's own name), a confidence signal, and a suggested area (derived from directory structure, not emitted as its own row type). Document-level: generator version, source repo path, generated-at timestamp, and source commit when available.
- FR-DIG-30 — README table mining: a markdown table with a spec/scenario/test-shaped header (case-insensitive substring match on the header cell, not a fixed vocabulary) yields candidate rows from its body — a spec-shaped column yields a US candidate, a scenario-shaped column yields one AC candidate per entry (split on `·`/`;`), and a test-shaped column resolves to a real `file:line` when a matching declaration exists under the target, attached as each scenario row's `candidateProof` (`basis: "matched"`). Minted 2026-08-22 (`docs/dig-level-two-handoff-v2-2026-08-22.md` §2a), citing the found gap from the insurance-java honest-usefulness assessment: the no-LLM floor's heading-only README mining left the repo's richest signal, its hand-maintained Spec → Scenario → Test coverage table, completely unread.
  - AC-DIG-60 — Given a markdown table with spec, scenario, and test columns naming real tests under the target, when `dig` runs, then every spec row and every scenario yields a candidate row, and every scenario row's `candidateProof` resolves to the real declaring `file:line` of the named test — full recovery of the published table, not a partial read.
- FR-DIG-40 — `candidateProof` is first-class on every row (`{ test, file, line, basis }`, `basis` = `"same-artifact"` when the provenance test doubles as the candidate proof, or `"matched"` when a separate heuristic paired an intent with a test named elsewhere) — never something a viewer infers from `provenance.source == "test"`. Minted 2026-08-22 (`docs/dig-level-two-handoff-v2-2026-08-22.md` §1).
  - AC-DIG-40 — Given a test-heuristic row with no explicit `candidateProof` set, when the report is emitted, then `candidateProof` is `{ test, file, line, basis: "same-artifact" }` sourced from that row's own provenance; given a row with no known candidate proof at all, `candidateProof` is `null`, never omitted.
- FR-DIG-50 — Known-smoke noise: a small, named list of recognized framework-smoke-test name shapes (e.g. `contextLoads`) never gets dropped — every found test still becomes a row — but is pre-labeled `confidence: "low"` with a stated `confidenceReason`, so an anointment reviewer's two-second toss is pre-explained rather than something they work out themselves each time. Minted 2026-08-22 (`docs/dig-level-two-handoff-v2-2026-08-22.md` §2c), citing the same assessment: `contextLoads` (Spring Boot's own generated smoke test) surfaced as real, correctly-shaped noise in the insurance-java run.
  - AC-DIG-50 — Given a test name matching a known framework-smoke-test shape, when `dig` runs, then that row's `confidence` is `"low"` and `confidenceReason` states the reason; an ordinary test row carries neither a lowered confidence nor a `confidenceReason` key.
- FR-DIG-60 — `candidateBuild` first-class on every row: the proof test's own project-package imports, filtered against a base package derived from the source tree itself, each surviving import resolved to a real declaration file. A file citation with import-line provenance, never a prose behavior claim. Registered 2026-09-03 for work shipped 2026-08-22 (`T918`, `docs/dig-level-three-handoff-v3-2026-08-22.md`): coverage registered, not newly attributed. The spec, task, and `@covers` marks all cited this row before it existed here, which is what kept this repo's own Self Gate red from 2026-08-23 until this mint.
  - AC-DIG-70 — Given a row carrying a `candidateProof`, when `dig` runs, then that row's `candidateBuild` lists the declaration files the proof test imports from the target's own package, and a row whose proof imports nothing of the repo's own carries an empty list, never a guess.
- FR-DIG-70 — `rowId` (stable, ordering-derived) and `candidateParent` (a list, since inferred structure is not bound by the registry's single-parent law; the human picks at anointment) first-class on every row. Registered 2026-09-03 for work shipped 2026-08-22 (`T919`, `docs/dig-structure-emission-handoff-v3-1-2026-08-22.md`): coverage registered, not newly attributed, same finding as `FR-DIG-60`.
  - AC-DIG-80 — Given a scenario row mined from a README table, when `dig` runs, then its `candidateParent` names the `rowId` of the spec row from the same physical table row with basis `"table-adjacency"`, and every `rowId` is stable across reruns of an unchanged target.
- FR-DIG-80 — Docs read that is complete or says it is not. `dig_readme` reads README plus at most the first ten `docs/*.md` files, alphabetically, top level only, no recursion, and prints nothing about the bound. A brownfield repo with specs nested under `docs/**` gets a partial read that looks complete. v-next: either crawl `docs/**` recursively with a stated cap, or keep the bound and print exactly which files were read and how many were skipped, in the counts line and in the report's document-level metadata. Silence about the bound is the defect; the bound itself may stay. Anointed backlog, minted 2026-09-04 from the v0.4.13 release commission's dig verdict (a stranger with a docs-heavy brownfield repo and zero context): receipt is `scripts/dig.py`'s `sorted(docs_dir.glob("*.md"))[:10]` line and a run against this repo that read 11 of its 24 top-level docs files with no notice. <!-- specassay:stale-ok the record of what was true at v0.4.13; re-observing it would mean re-running on that release, which is not what the sentence claims -->
- FR-DIG-90 — Doc-derived rows that carry a reason, not just a heading. Every prose-sourced row today is a markdown heading's text typed as a US at `confidence: "low"`, with no `confidenceReason`; against this repo, 75 of 165 rows were such headings (`"What already works cleanly"` among them). v-next: mine sentence-shaped statements (requirement verbs, Given/When/Then, numbered acceptance lists, tables already handled by FR-DIG-30) ahead of headings, and give every low-confidence prose row a `confidenceReason` the way FR-DIG-50 already does for smoke tests, so a reader can sort signal from scaffolding without opening each file. Anointed backlog, minted 2026-09-04, same commission and receipt as FR-DIG-80.
- FR-DIG-100 — A path from `dig-report.json` to a first minted ID, without touching the hard law. `dig` never writes a registry and has no `--anoint` flag (FR-DIG-10); today the report is JSON to read by hand and nothing turns a chosen row into a minted line, which for a stranger is a dead end of the same shape the empty-registry green was before FR-GATE-100. v-next is a separate, human-driven step, not a dig flag: given a `rowId` from a report, `mint-id.sh` (or a small sibling) proposes the registry line from that row's statement and provenance and appends it only on explicit confirmation, with the row's `candidateProof` file and line printed alongside so the first test name to write or rename is in front of the person. Design question settled 2026-09-04 (Rik, recorded on `T921`): the minted line records its origin in the intent PR and merge commit, never in the registry row; origin is a fact of the mint event, not a property of the promise. Anointed backlog, minted 2026-09-04, same commission; until it ships, the manual mint-first-ID on-ramp the Gate prints (FR-GATE-100) is the brownfield instruction.

## THREAD

The Thread Report: the illuminate rung, one comment per pull request. It
diffs a base trace-manifest against the PR head's and briefs a reviewer on
what the change did to the thread. It never refuses and never blocks; the
refusal is a separate CI step.

Founded 2026-09-17, on a ruling: a shipped tool this repo's own registry did
not govern was the assay office running an ungoverned instrument. What is
minted here is the **display contract** only — what a reader gets in five
seconds, what folds, and what never folds — because the display is the promise
a reader can hold us to. Governing the tool's behaviour more broadly is a
promise not yet decided, and minting it now would be the umbrella row declined
at the 0.5.0 mint. It waits until there is behaviour worth naming.

- FR-THREAD-10 — The report is readable in one glance and complete under one click. It opens with a single verdict line carrying the thread's state and the counts that say what this change did — rows proved, rows moved to admitted debt, changed files off thread, and any of minted, retired, restated, moved to GAP, or carrier-added-status-held that actually occurred, each named only when its count is non-zero. Everything else renders inside a `<details>`: the moved rows, the off-thread file list, and any caller-supplied receipt (`--receipts`). Collapsing is not dropping — every fact the report has ever carried stays reachable, including retired IDs, which have no head row to belong to. Each moved row is stated **once**: the family tables carry both the move and the state, so no fact appears as a bullet and again as a table row. Rows a change did not move are footnoted with their status counts rather than listed, so a report about this change stays about this change without losing the state around it. **Two things never fold**, whatever else does: *Intent Changed*, because a restated promise asks the reader to go and re-confirm something, and a required human tick (`offthread_ack` / `intent_ack: required`), because it holds a merge — a warning behind a click is a warning nobody read. Minted 2026-09-17 from the first Thread Report to render on a real estate (Chalkup `CHA-23`), which stated every moved row twice, listed unmoved rows beside moved ones, and left the gate log that produced it open at the bottom.
  - AC-THREAD-10 — Given a base and head manifest and a PR's changed files, when the report renders, then its second non-empty line is a single verdict line naming the thread's state and the non-zero counts (proved, admitted debt, off-thread files, and any other move that occurred); each moved row is stated exactly once, in a family table carrying both its move and the files this PR changed that carry it; rows that did not move appear only as a footnoted count by status; the moved rows, the off-thread list and any `--receipts` content each render inside a `<details>`; and *Intent Changed* and any `required` human tick render outside every `<details>`, with no fold open above them.

## SELF

One bounded meta-row, minted 2026-08-18: retroactive coverage for this
repo's own pre-existing behavior. Burns down over time; explicitly does not
block DOCS-room work.

- FR-SELF-10 — Backfill `@covers` marks across specassay's own existing extension/preset behavior that already works but was never annotated.

## CASE

The objective tier, ratified by Rik 2026-09-22 in one line: "Objectives are
minted in CASE.md, cited by stories, refused when orphaned, and the manifest
carries them." This section is that ruling written as intent, minted before
any code, in the practice's own order. The area is named for the document
rather than for the tier deliberately: an area named `OBJ` would sit beside
the `OBJ-` prefix these rows introduce, and `FR-CASE-30` is the row that
forbids reading an edge off an ID family. Naming the two the same thing would
invite the inference it refuses.

Nothing below is proven by declaration. Every row opens as anointed backlog
carried by `T929` in `specs/backlog/tasks.md`, per this registry's founding
scope.

- US-CASE-10 — As someone accountable for why the work exists, I want every story to name the objective it serves, so the thread runs from an outcome I can be held to all the way down to the test that proves it, and a story serving nothing is visible rather than merely unremarked. Today the thread starts at the story: a registry can be entirely green while nobody can say what any of it was for. The tier above story is where that question has an answer that a tool can check, and the reason it belongs in the same machinery rather than in a slide is that an unchecked objective drifts from the work silently, which is the failure this whole tool exists to refuse one level down. **The two documents hold different kinds of thing, ratified 2026-09-22, and reading them alike is the mistake this row exists to prevent.** `CASE.md` holds **bets**, scored later by measures in the world. `PRD.md` holds **promises**, proven by named tests. So a green objective means its promises were kept, not that the bet paid: the CASE paraphrases, the PRD rules. `FR-CASE-60` already says an objective's proof is a question about the world; this says whose question it is, so nobody reads green as paid.
- FR-CASE-10 — A fourth tier above story: **objective**, with IDs of the form `OBJ-<DOMAIN>-NN`, **assigned by the Gate, never typed by a person.** A `CASE.md` author writes headings and prose and nothing else; the first Gate pass that reads an objective without an ID assigns one and **writes it back into the file**, in the same pull-request-visible way `trace-manifest.json` is written. Ruled 2026-09-22, reversing this row's first draft, which had `mint-id.sh` mint objectives the way it mints into `PRD.md`. The reason is the document's audience: a CASE is written by whoever is accountable for the bet, in prose, and asking that person to run a minter before they can finish a sentence puts the registry's machinery in front of the thinking the document exists to hold. A durable ID is still durable; what changes is who types it. **`mint-id.sh` is not the objective minter** and is unchanged by this row: it stays exactly as it is for the `PRD.md` registry. Two things this still touches. The stock `id_regex` admits only `(FR|NFR|AC|US)` as a prefix, so `OBJ-LEND-10` is an ID the current Gate would refuse on sight; the grammar has to admit the new prefix, and a project that has configured its own grammar has to be able to admit it too, per the class settled in 0.5.0 that a configured grammar is consumed as configured. And a Gate that writes back into a source document is new behaviour for this tool, which has only ever written artifacts: the write has to be as reviewable as any other diff, which is why it lands in the working tree for a human to commit rather than anywhere else.
  - AC-CASE-10a — Given a `CASE.md` objective heading carrying no ID, when the Gate runs, then it assigns the next `OBJ-<DOMAIN>-NN` for that domain and writes the ID back into `CASE.md`, so the file on disk afterwards carries the ID the manifest reports.
  - AC-CASE-10b — Given a `CASE.md` objective that already carries an ID, when the Gate runs, then that ID is kept exactly as written and the file is not rewritten on account of it.
  - AC-CASE-10c — Given a `CASE.md` whose objectives are reordered, renamed, or interleaved with new ones, when the Gate runs, then every existing objective keeps the ID it already had and only the objectives without one are assigned to: **the Gate never renumbers.**
- FR-CASE-20 — A second registry path in the config (`CASE.md` beside `PRD.md`), read by the Gate under the same exact-set rules one level up. The rule that binds registry, specs and tasks at the story tier binds objectives and stories at the tier above it, with the same two-sided refusal: **a story that names no objective is an orphan**, and **an objective that no story names is backlog, visible, not a gap**. That asymmetry is the whole design and is stated here so nobody softens it later: an unserved objective is a promise nobody has started, which is honest and common, while a story serving nothing is work whose reason has gone missing, which is the condition worth surfacing. Reading a second document is not the same as reading the first one twice: the two registries have different tiers, different mint sites and different owners, and the config has to say which is which rather than leaving the Gate to infer it from a filename. **Objectives are read by heading, under the CASE template's "What are you betting?" question, and any numbering a person adds to those headings is decoration the reader ignores** (ruled 2026-09-22). The template's nine questions are headings; whether a given `CASE.md` numbers them is a house style, and a registry that keyed on the number would make one house style a requirement and silently lose every objective written in the other.
  - AC-CASE-20a — Given a config naming both an ID registry and an objective registry, when the Gate runs, then it reads both, and the set of objectives it knows is exactly the set defined in the objective registry, with the same duplicate and orphan refusals the ID registry already gets.
  - AC-CASE-20b — Given an objective that no story cites, when the Gate runs, then that objective is carried as `backlog` and named in the run's own output, and the run does not fail on account of it.
  - AC-CASE-20c — Given two `CASE.md` files identical except that one numbers its question headings and the other does not, when the Gate runs on each, then both produce the same objective rows, with the same IDs and the same statements.
- FR-CASE-30 — The citation is declared, never inferred. A story names its objective either by a declared line in the spec (`Serves: OBJ-LEND-10`, parallel to `**Carries**:` and `**Retires**:`) or by nesting under it in the registry document, and by nothing else. **No edge is ever read off an ID family:** `US-LEND-10` and `OBJ-LEND-10` share a domain and that is all they share. This is the same rule `FR-GATE-90` already holds for parent derivation, where the reason was written down at the time: an inferred edge is a guess that renders identically to a fact. The domain-grouping convention remains a display fallback for a reader with no edges, and never becomes an edge.
  - AC-CASE-30a — Given a spec carrying a well-formed `Serves:` line naming one or more objectives, when the Gate runs, then the story's objective edges are exactly the objectives that line names.
  - AC-CASE-30b — Given a registry document where a story is nested under an objective, when parent derivation is enabled, then the objective is the story's parent by the same nesting rule every other tier already uses.
  - AC-CASE-30c — Given a story and an objective sharing a domain, with no `Serves:` line and no nesting between them, when the Gate runs, then no edge is derived between them and the story is treated as serving no objective.
- FR-CASE-40 — The refusal for an orphan story ships **report-only**, in `gate.diagnostics[]`, and ratchets to blocking per project the way `block_uncovered_proof` already does. The reasoning is the same as it was there, and it is not timidity: a real registry carries a backlog of orphan stories the first time this runs, because the tier did not exist when those stories were written. A check that reds every adopter on first contact is a check they turn off, and a check nobody runs refuses nothing. Report-only makes the backlog visible and countable; the per-project switch is how an estate that has burned the backlog down keeps it down. This row promises both halves: the diagnostic now, and the switch that makes it a refusal on a project's own word.
  - AC-CASE-40a — Given a story naming no objective, when the Gate runs with the objective registry configured and the ratchet unset, then the run records the orphan story in `gate.diagnostics[]`, names the count in its output, and `gate.ok` is unchanged by it.
  - AC-CASE-40b — Given the same project with the ratchet set, when the Gate runs, then an orphan story is a failing finding and the run fails.
- FR-CASE-50 — The manifest carries objective rows: `tier: "objective"`, parents from the citation rather than from the document alone, and a rollup computed by the ratified colour rule. **The rule, ratified 2026-09-22, written here so viewers do not each invent one:** a parent's colour is the weakest *started* child among `GAP`, `tracked-debt` and `proven`, in that order of weakness; `backlog` and `retired` are counted but not coloured; and a parent with no started child is `backlog`. The rule goes into `docs/trace-manifest-v5.md`'s rollup section, which is where a viewer looks, and not only into this row. This matters more than it looks: the rollup already present is a count, and a count does not tell a renderer what colour to paint. Every viewer that has wanted one has so far had to decide for itself, which is how two renderings of one manifest come to disagree, and a manifest whose readers disagree is the capture-method failure `PROMOTION-CONTRACT.md` rule 12 names.
  - AC-CASE-50a — Given a project with objectives and stories citing them, when the Gate emits the manifest, then each objective is a row with `tier: "objective"` whose children are exactly the stories that cite it, by the citation and not by domain.
  - AC-CASE-50b — Given an objective whose started children include at least one `GAP`, when the rollup is computed, then the objective's rolled-up colour is `GAP`; given started children that are all `proven`, then `proven`; given no started child at all, then `backlog`, with `backlog` and `retired` children counted in the totals either way.
  - AC-CASE-50c — Given `docs/trace-manifest-v5.md`, when read at the rollup section, then the colour rule is stated there in full, so a viewer implementing it needs no access to this registry.
- FR-CASE-60 — Objective-level proof may be a **measure** rather than a test: a key result with a named source, the way an acceptance criterion's proof is a named passing test. **Named here as a later ID, deliberately not built**, so that the tier's shape does not exclude it. The constraint this places on `FR-CASE-10` through `FR-CASE-50` is the whole reason it is minted now rather than later: nothing in the tier's first build may assume that proof is a test, that a proof lives in a test file, or that an unproven objective is awaiting a test. An objective's proof is a question about the world, and the world is not in the repository. What a source is, how a measure is captured, and how stale a measure may be before it stops counting are all open and are not promised by this row.

## REL

Release cut lines, ratified in principle 2026-09-22 and **sequenced behind
the objective tier**, not shipping with it. Minted in the same PR for intent
so the two are read together, because a release name is a second axis across
the same rows and the two designs constrain each other; built separately,
because the tier is the harder half and this one is cheap once the row shape
has settled.

- FR-REL-10 — A registry row may carry `Release: <name>`. The registry holds the short list of release names it recognises; the Gate checks the tag exact-set against that list, the same way it checks every other set it reads, so a row tagged for a release nobody declared is a refusal rather than a new release; the manifest carries the release as a row field; and **a row with no release is unscheduled backlog**, which is a statement about the plan rather than about the work. A free-text tag would make a typo into a silent second release, and a release that exists because somebody misspelled one is exactly the kind of quiet drift the exact-set rule exists to catch. Deliberately not promised here: any ordering between releases, any date, and any notion of a release being complete. This row promises a name, a closed list, a check and a field.
  - AC-REL-10a — Given a registry declaring its release names, when a row carries a `Release:` naming one of them, then the manifest's row for that ID carries that release name.
  - AC-REL-10b — Given a row carrying a `Release:` naming a release the registry does not declare, when the Gate runs, then it fails with a finding naming the row and the undeclared name.
  - AC-REL-10c — Given a row carrying no `Release:` at all, when the Gate runs, then the run does not fail on account of it and the row is reported as unscheduled.
