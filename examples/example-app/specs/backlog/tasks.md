# Backlog — Anointed (minted on purpose, not yet picked up)

Anointed backlog = a registry ID whose **only** carrier is an open `- [ ]`
TODO. It is deliberately *not* in any spec, has no `@covers` mark, and no
`test_AC_*` proof. The open TODO below is what proves the intent is minted and
keeps Gate 2 from flagging it as drift. Pick it up end-to-end and watch the
status flip from `backlog` → `proven`.

- [ ] T900 Deliver inline-edit undo end-to-end — **Carries**: AC-EDIT-01 (anointed backlog)
